{
  "configurations": [
  {
    "name": "CUDA C++: Launch",
    "type": "cuda-gdb",
    "request": "launch",
    "program": "${workspaceFolder}/out",
    "args": ["-i","${workspaceFolder}/../test/m_8.txt","-v" ,"-mode", "0"]
  }
  ]
}
